<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="iconBackArrow">@drawable/ic_back_arrow</item>
        <item name="colorInAccent">@color/colorInAccent</item>
    </style>

    <!-- Toolbar -->
    <style name="Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">?attr/colorAccent</item>

        <item name="android:homeAsUpIndicator">@drawable/ic_back_arrow</item>
        <item name="collapseIcon">@drawable/ic_search</item>

        <item name="android:popupTheme">@style/ThemeOverlay.AppCompat.ActionBar</item>
    </style>

    <style name="SearchView" parent="Widget.AppCompat.SearchView.ActionBar">

    </style>

</resources>
